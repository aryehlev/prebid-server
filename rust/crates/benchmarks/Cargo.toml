[package]
name = "benchmarks"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
tokio = { workspace = true }

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }
experimental-simd-json = { path = "../experimental-simd-json" }
analytics = { path = "../analytics" }
pbs-privacy = { path = "../privacy" }

[[bench]]
name = "request_parse"
harness = false

[[bench]]
name = "gpp_parse"
harness = false

[[bench]]
name = "analytics_fanout"
harness = false
