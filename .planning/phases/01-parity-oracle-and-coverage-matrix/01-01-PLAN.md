---
phase: 01-parity-oracle-and-coverage-matrix
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - parity/contracts.go
  - parity/oracle/auction_test.go
  - parity/oracle/categorize_test.go
  - parity/matrix/matrix_test.go
  - parity/testdata/oracle/approved-normalization.json
  - parity/testdata/matrix/domains.json
autonomous: true
requirements:
  - PARI-01
  - PARI-02
must_haves:
  truths:
    - "Phase executors have one shared contract for domains, mismatch records, evidence links, and auction cases before live oracle code begins."
    - "Wave-0 parity tests exist for both the auction oracle and the coverage matrix, so later plans can prove behavior with `go test ./parity/...`."
    - "The locked domain list is encoded once and reused by both mismatch categorization and matrix generation."
  artifacts:
    - path: "parity/contracts.go"
      provides: "Shared parity contracts"
      contains: "type Domain string"
    - path: "parity/oracle/auction_test.go"
      provides: "Wave-0 auction oracle scaffold"
      contains: "func TestAuctionOracle"
    - path: "parity/matrix/matrix_test.go"
      provides: "Wave-0 coverage matrix scaffold"
      contains: "func TestCoverageMatrix"
  key_links:
    - from: "parity/contracts.go"
      to: "parity/oracle/categorize_test.go"
      via: "shared mismatch and domain schema"
      pattern: "MismatchRecord|AllDomains"
    - from: "parity/contracts.go"
      to: "parity/matrix/matrix_test.go"
      via: "locked matrix row order"
      pattern: "AllDomains"
---

<objective>
Establish the shared parity contracts, locked domain taxonomy, and Wave-0 validation scaffolding that every later Phase 1 plan will build on.

Purpose: Lock the user decisions into code and tests before any live Go-vs-Rust comparison logic is implemented.
Output: Shared parity contracts, approved testdata fixtures, and compile-safe Wave-0 tests for PARI-01 and PARI-02.
</objective>

<execution_context>
@/Users/aryehlev/.codex/get-shit-done/workflows/execute-plan.md
@/Users/aryehlev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-parity-oracle-and-coverage-matrix/01-CONTEXT.md
@.planning/phases/01-parity-oracle-and-coverage-matrix/01-RESEARCH.md
@.planning/phases/01-parity-oracle-and-coverage-matrix/01-VALIDATION.md
@.planning/codebase/TESTING.md
@.planning/codebase/STRUCTURE.md
@endpoints/openrtb2/test_utils.go
@endpoints/openrtb2/auction_test.go
@rust/crates/endpoints/src/lib.rs

<interfaces>
From `endpoints/openrtb2/test_utils.go`:
```go
type testCase struct {
	Description                string
	BidRequest                 json.RawMessage
	ExpectedValidatedBidReq    json.RawMessage
	ExpectedMockBidderRequests map[string]json.RawMessage
	ExpectedReturnCode         int
	ExpectedErrorMessage       string
	ExpectedBidResponse        json.RawMessage
}

func parseTestData(fileData []byte, testFile string) (testCase, error)
```

From `endpoints/openrtb2/auction_test.go`:
```go
func runJsonBasedTest(t *testing.T, filename, desc string)
func compareWarnings(t *testing.T, expectedBidResponseExt, actualBidResponseExt []byte, warnPath string)
```

From `rust/crates/endpoints/src/lib.rs`:
```rust
pub type AppState = Arc<AppStateInner>;
pub fn create_router(state: AppState) -> axum::Router
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Define shared parity contracts and locked domains</name>
  <read_first>
    .planning/phases/01-parity-oracle-and-coverage-matrix/01-CONTEXT.md
    .planning/phases/01-parity-oracle-and-coverage-matrix/01-VALIDATION.md
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
  </read_first>
  <files>parity/contracts.go, parity/testdata/matrix/domains.json</files>
  <action>Create `parity/contracts.go` with the shared types consumed by every later plan: `type Domain string`, `type SurfaceStatus string`, `type EvidenceLink struct`, `type MismatchRecord struct`, `type MatrixRow struct`, and `type AuctionCase struct`. Encode the locked domain list exactly once, using these exact domain IDs in this exact order per D-05 and D-06: `endpoints`, `config_bootstrap`, `stored_data`, `auction_lifecycle`, `policy_privacy_identity`, `bidders_adapters`, `hooks_modules`, `cache_support_flows`, `cutover_proof`. Add `const SampleRequestRoot = "endpoints/openrtb2/sample-requests"` so later loader code can reject traversal per T-01-01. Add `parity/testdata/matrix/domains.json` as the canonical expected-row fixture. Do not add phase-only or requirement-only rows.</action>
  <acceptance_criteria>
    - `rg -n 'type Domain string' parity/contracts.go` finds the shared domain contract.
    - `rg -n 'config_bootstrap|auction_lifecycle|cutover_proof' parity/contracts.go parity/testdata/matrix/domains.json` finds the locked domain set.
    - `rg -n 'SampleRequestRoot' parity/contracts.go` finds the approved fixture-root constant.
  </acceptance_criteria>
  <verify>
    <automated>go test ./parity/... -run TestCoverageMatrix -count=1</automated>
  </verify>
  <done>The shared contracts compile, the locked domains exist once, and later plans can import them without redefining domain taxonomy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Create Wave-0 parity test scaffolding and approved samples</name>
  <read_first>
    .planning/phases/01-parity-oracle-and-coverage-matrix/01-VALIDATION.md
    .planning/codebase/TESTING.md
    endpoints/openrtb2/auction_test.go
    exchange/exchange_test.go
  </read_first>
  <files>parity/oracle/auction_test.go, parity/oracle/categorize_test.go, parity/matrix/matrix_test.go, parity/testdata/oracle/approved-normalization.json</files>
  <behavior>
    - Test 1: `TestAuctionOracle` fails if the oracle cannot load an approved fixture case, cannot invoke both runtimes, or does not emit categorized mismatches for status, warnings, and response body per D-01, D-02, and D-07.
    - Test 2: `TestAuctionOracleRejectsTraversal` fails if a case path escapes `SampleRequestRoot`, covering T-01-01.
    - Test 3: `TestCoverageMatrix` fails if any locked domain row is missing, out of order, or lacks evidence-link fields per D-05, D-06, and D-08.
  </behavior>
  <action>Create the Wave-0 scaffolding required by `01-VALIDATION.md`: `parity/oracle/auction_test.go`, `parity/oracle/categorize_test.go`, `parity/matrix/matrix_test.go`, and `parity/testdata/oracle/approved-normalization.json`. Keep the tests table-driven and executable with `go test ./parity/...`. Add the smallest compile-safe production placeholders needed so tests fail for missing behavior instead of missing symbols. Make the normalization sample cover warning order or seatbid order only, and do not include any broader normalization cases per D-03 and D-04.</action>
  <acceptance_criteria>
    - `rg -n 'func TestAuctionOracle' parity/oracle/auction_test.go` finds the oracle scaffold.
    - `rg -n 'RejectsTraversal|approved-normalization' parity/oracle/auction_test.go parity/testdata/oracle/approved-normalization.json` finds traversal and normalization coverage.
    - `rg -n 'func TestCoverageMatrix' parity/matrix/matrix_test.go` finds the matrix scaffold.
  </acceptance_criteria>
  <verify>
    <automated>go test ./parity/... -run 'TestAuctionOracle|TestCoverageMatrix' -count=1</automated>
  </verify>
  <done>Wave-0 parity tests and approved samples exist, compile, and encode the locked Phase 1 decisions before live implementation begins.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture file path -> parity loader | Untrusted path input can attempt traversal outside the approved sample-request corpus |
| testdata fixture -> matrix/oracle assertions | Incorrect domain fixtures can silently erase required parity rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T | `parity/contracts.go` fixture-root contract | mitigate | Define a single `SampleRequestRoot` constant and require later loader code to clean paths and reject any path outside that prefix |
| T-01-02 | T | `parity/testdata/matrix/domains.json` | mitigate | Encode the exact locked domain IDs from D-05/D-06 in one canonical fixture and make `TestCoverageMatrix` fail if rows are missing or reordered |
| T-01-03 | I | Wave-0 parity samples | mitigate | Keep `approved-normalization.json` limited to repo fixture content and normalization metadata only; do not introduce raw external traffic payloads in committed testdata |
</threat_model>

<verification>
Run `go test ./parity/... -run 'TestAuctionOracle|TestCoverageMatrix' -count=1` and confirm the new package compiles with the Wave-0 tests in place.
</verification>

<success_criteria>
The phase has a reusable parity contract layer, the locked domain taxonomy is codified once, and the validation-required Wave-0 tests exist before any live oracle implementation starts.
</success_criteria>

<output>
After completion, create `.planning/phases/01-parity-oracle-and-coverage-matrix/01-01-SUMMARY.md`
</output>
