---
phase: 01-parity-oracle-and-coverage-matrix
plan: "03"
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - parity/matrix/matrix.go
  - parity/matrix/render.go
  - parity/matrix/matrix_test.go
  - parity/cmd/update-matrix/main.go
  - parity/matrix/status.json
  - parity/matrix/README.md
autonomous: false
requirements:
  - PARI-02
must_haves:
  truths:
    - "A domain-level compatibility matrix exists for all locked surfaces, not just the auction slice."
    - "The current auction-first oracle evidence is linked into the matrix rows that it actually covers."
    - "Non-instrumented domains are explicit, ordered, and readable enough for later phases to route mismatches without guesswork."
  artifacts:
    - path: "parity/matrix/matrix.go"
      provides: "Matrix builder and row-status logic"
      contains: "func BuildMatrix"
    - path: "parity/matrix/status.json"
      provides: "Machine-readable compatibility matrix snapshot"
      contains: "\"domain\""
    - path: "parity/matrix/README.md"
      provides: "Human-readable matrix snapshot"
      contains: "not-yet-instrumented"
  key_links:
    - from: "parity/out/auction/index.json"
      to: "parity/matrix/status.json"
      via: "sanitized evidence links"
      pattern: "artifact_path"
    - from: "parity/matrix/status.json"
      to: "parity/matrix/README.md"
      via: "deterministic renderer"
      pattern: "BuildMatrix|RenderMatrix"
---

<objective>
Generate the Phase 1 compatibility matrix and rendered snapshot from the oracle evidence while keeping every locked domain visible from day one.

Purpose: Deliver PARI-02 as an explicit artifact that later phases can update rather than redesign.
Output: Machine-readable and human-readable matrix snapshots plus a final readability checkpoint.
</objective>

<execution_context>
@/Users/aryehlev/.codex/get-shit-done/workflows/execute-plan.md
@/Users/aryehlev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-parity-oracle-and-coverage-matrix/01-CONTEXT.md
@.planning/phases/01-parity-oracle-and-coverage-matrix/01-RESEARCH.md
@.planning/phases/01-parity-oracle-and-coverage-matrix/01-VALIDATION.md
@parity/contracts.go
@parity/oracle/categorize.go
@parity/out/auction/index.json
@parity/matrix/matrix_test.go

<interfaces>
From `parity/contracts.go`:
```go
type MatrixRow struct {
    Domain       Domain
    Status       SurfaceStatus
    EvidenceLinks []EvidenceLink
}
```

From the Phase 1 locked decisions:
```text
All matrix rows must stay domain-level.
Initial rows must cover endpoints, config/bootstrap, stored data, auction lifecycle,
policy/privacy/identity, bidders/adapters, hooks/modules, cache/support flows, and cutover proof.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the domain-level matrix from oracle evidence</name>
  <read_first>
    parity/contracts.go
    parity/matrix/matrix_test.go
    parity/out/auction/index.json
    .planning/phases/01-parity-oracle-and-coverage-matrix/01-CONTEXT.md
  </read_first>
  <files>parity/matrix/matrix.go, parity/matrix/matrix_test.go</files>
  <behavior>
    - Test 1: `TestCoverageMatrix` fails if `BuildMatrix` omits any locked domain or changes the locked order from `AllDomains`.
    - Test 2: Auction evidence from `parity/out/auction/index.json` populates only the rows actually covered now, while untouched domains remain `not-yet-instrumented` per D-08.
    - Test 3: Matrix rows fail validation if they omit evidence-link fields or attempt to embed raw response bodies.
  </behavior>
  <action>Create `parity/matrix/matrix.go` with `func BuildMatrix(indexPath string) ([]MatrixRow, error)` and any small helpers needed for status calculation. Use exactly these statuses: `passing`, `mismatch`, and `not-yet-instrumented`. Map current auction-slice evidence into `endpoints` and `auction_lifecycle` rows when appropriate, and leave the remaining locked domains present with `not-yet-instrumented` plus a short rationale. Refuse any row that lacks evidence-link metadata or includes raw body payloads, covering T-01-06 and T-01-07.</action>
  <acceptance_criteria>
    - `rg -n 'func BuildMatrix' parity/matrix/matrix.go` finds the matrix builder.
    - `rg -n 'passing|mismatch|not-yet-instrumented' parity/matrix/matrix.go` finds the allowed status set.
    - `rg -n 'cutover_proof|cache_support_flows|hooks_modules' parity/matrix/matrix.go parity/matrix/matrix_test.go` finds the locked domain coverage.
  </acceptance_criteria>
  <verify>
    <automated>go test ./parity/... -run TestCoverageMatrix -count=1</automated>
  </verify>
  <done>The matrix builder always emits the full locked domain list, wires current oracle evidence into the correct rows, and makes non-instrumented surfaces explicit.</done>
</task>

<task type="auto">
  <name>Task 2: Render deterministic matrix snapshots for planners and executors</name>
  <read_first>
    parity/matrix/matrix.go
    parity/out/auction/index.json
    .planning/ROADMAP.md
    .planning/phases/01-parity-oracle-and-coverage-matrix/01-VALIDATION.md
  </read_first>
  <files>parity/matrix/render.go, parity/cmd/update-matrix/main.go, parity/matrix/status.json, parity/matrix/README.md</files>
  <action>Create `parity/matrix/render.go` plus `parity/cmd/update-matrix/main.go` so `go run ./parity/cmd/update-matrix` reads `parity/out/auction/index.json`, calls `BuildMatrix`, writes `parity/matrix/status.json`, and renders `parity/matrix/README.md`. Keep the output deterministic and repo-friendly: JSON rows must include `domain`, `status`, `summary`, and `evidence_links`; the Markdown snapshot must list every locked domain row and show `not-yet-instrumented` for untouched surfaces rather than hiding them. The rendered files must not include raw response bodies or shell commands.</action>
  <acceptance_criteria>
    - `rg -n 'domain|status|evidence_links' parity/matrix/status.json` finds the machine-readable snapshot fields.
    - `rg -n 'not-yet-instrumented|auction_lifecycle|config_bootstrap' parity/matrix/README.md` finds the human-readable snapshot rows.
    - `rg -n 'package main|update-matrix' parity/cmd/update-matrix/main.go` finds the snapshot generator entrypoint.
  </acceptance_criteria>
  <verify>
    <automated>go test ./parity/... -run TestCoverageMatrix -count=1 && go run ./parity/cmd/update-matrix</automated>
  </verify>
  <done>The repo contains current machine-readable and human-readable matrix snapshots generated from the auction oracle evidence and the locked domain list.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm matrix readability for later phase routing</name>
  <read_first>
    parity/matrix/README.md
    parity/matrix/status.json
    .planning/ROADMAP.md
    .planning/phases/01-parity-oracle-and-coverage-matrix/01-VALIDATION.md
  </read_first>
  <files>parity/matrix/README.md, parity/matrix/status.json</files>
  <action>Generate the latest matrix snapshot with `go run ./parity/cmd/update-matrix`, then pause for inspection of `parity/matrix/README.md`. Do not continue until the user confirms the report is readable enough to route mismatches into later roadmap phases per D-05, D-06, and D-08.</action>
  <acceptance_criteria>
    - `test -f parity/matrix/README.md` succeeds.
    - `rg -n 'endpoints|config_bootstrap|stored_data|auction_lifecycle|policy_privacy_identity|bidders_adapters|hooks_modules|cache_support_flows|cutover_proof' parity/matrix/README.md` finds every locked domain row.
    - `rg -n 'artifact|evidence' parity/matrix/README.md parity/matrix/status.json` finds evidence references without raw payload dumps.
  </acceptance_criteria>
  <what-built>Domain-level matrix snapshots with current auction evidence and explicit placeholders for every remaining parity surface.</what-built>
  <how-to-verify>
    1. Open `parity/matrix/README.md`.
    2. Confirm all nine locked domain rows are present in roadmap order.
    3. Confirm `endpoints` and `auction_lifecycle` rows link to current oracle evidence, while untouched domains are marked `not-yet-instrumented`.
    4. Confirm a mismatch could be assigned to a later phase without manually reinterpreting the diff.
  </how-to-verify>
  <resume-signal>Type `approved` if the matrix is readable, or describe the routing gap that needs to be fixed.</resume-signal>
  <verify>
    <automated>go test ./parity/... -run TestCoverageMatrix -count=1 && go run ./parity/cmd/update-matrix</automated>
  </verify>
  <done>The user confirms the matrix snapshot is readable enough to route parity work into later phases without guesswork.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| oracle index -> matrix builder | Untrusted or malformed oracle evidence can drop rows or inject unsanitized content into summaries |
| matrix snapshot -> human planning workflow | Missing domains or opaque summaries can cause misrouting of parity work even when raw evidence exists |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | T | `parity/matrix/matrix.go` | mitigate | Build rows from `AllDomains` in locked order and fail tests when any domain is missing, reordered, or silently collapsed |
| T-01-08 | I | `parity/matrix/status.json` and `parity/matrix/README.md` | mitigate | Render summaries and evidence metadata only; reject raw response bodies and other payload dumps in matrix rows |
| T-01-09 | R | Human-readable matrix snapshot | mitigate | Keep deterministic JSON and Markdown snapshots plus the blocking readability checkpoint so planners can verify the routing artifact actually matches the roadmap |
</threat_model>

<verification>
Run `go test ./parity/... -run TestCoverageMatrix -count=1 && go run ./parity/cmd/update-matrix`, then complete the readability checkpoint against `parity/matrix/README.md`.
</verification>

<success_criteria>
Phase 1 ends with a committed compatibility matrix artifact that shows the current auction parity slice, preserves every future parity surface as an explicit row, and is readable enough to drive later phase execution.
</success_criteria>

<output>
After completion, create `.planning/phases/01-parity-oracle-and-coverage-matrix/01-03-SUMMARY.md`
</output>
